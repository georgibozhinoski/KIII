apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: course-suggestion
spec:
  selector:
    app: db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None  # Headless service — required for StatefulSets
